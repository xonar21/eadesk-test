global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "signal-lab-backend"
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:3001"]
        labels:
          app: "signal-lab"
          component: "backend"
